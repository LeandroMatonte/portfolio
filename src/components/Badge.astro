---
const { title, id } = Astro.props;
---

<span
	class="inline-block py-2 px-4 rounded font-bold text-center text-sm color-[var(--badge-color)] bg-[var(--badge-background)]"
	transition:name={`badge-${id}`}
>
	{title}
</span>
